{
  "name": "irrelon-json-to-csv",
  "version": "1.0.0",
  "description": "Converts one or more JSON files to CSV files based on dot notation paths to data.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "irrelon-path": "^1.0.2"
  }
}
